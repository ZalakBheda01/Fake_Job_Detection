# Fake Job Detection

Streamlit frontend for the Fake Job Detection machine-learning project.

## Run
pip install -r requirements.txt
streamlit run app.py

## Model
Place `fake_job_detection_logistic_regression.pkl` inside the `model` folder.
